This is a simple Tetris game with holding functionality.

To make it work as intended, you must set up a JDBC database on your system.

If you wish to bypass the database, compile all files and run SimpleStarter.class